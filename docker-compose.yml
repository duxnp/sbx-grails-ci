services:
  web:
    image: kowalb038/sbx-grails-ci:0.2.0 # x-release-please-version
    container_name: ciexperiments
    ports:
      - 27200:8080
    environment:
      TZ: 'America/New_York'
      NLS_LANG: '.AL32UTF8'
    env_file:
      - .env
    restart: unless-stopped
